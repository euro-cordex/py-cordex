name: py-cordex-tests
channels:
  - conda-forge
  - nodefaults
dependencies:
  - cartopy==0.20.2
  - proj==9.0.0
  - pyproj==3.3.0
  - matplotlib-base
  - numpy
  - pooch
  - regionmask
  - setuptools
  - xarray
  - cftime
  - netCDF4
  - xesmf
  - dask
  - cdo==1.9.9 # python-cdo fails with cdo2
  - python-cdo
# for testing
  - pytest
  - pytest-cov
  - pytest-env
