name: py-cordex-tests
channels:
  - conda-forge
  - nodefaults
dependencies:
  - cartopy
  - matplotlib-base
  - numpy<1.24 # fixed until next cmor release
  - pooch
  - regionmask
  - setuptools
  - xarray
  - cftime
  - netCDF4
  - xesmf
  - dask
  - cdo # python-cdo fails with cdo2
  - python-cdo
  - cmor
# for testing
  - pytest
  - pytest-cov
  - pytest-env
